{
  "openapi": "3.1.0",
  "info": {
    "title": "Webhook Example",
    "version": "1.0.0"
  },
  "security": [
    {
      "Signature": [],
      "X-Auth-Code": []
    }
  ],
  "webhooks": {
    "newPet": {
      "post": {
        "parameters": [
          {
            "name": "Signature",
            "in": "header",
            "required": true,
            "schema": {
              "type": "string"
            },
            "description": "HMAC-SHA256 signature of the raw request body, signed with the merchant secret key and hex-encoded.",
            "example": "5d41402abc4b2a76b9719d911017c592ab8562ec7b0122c79fc29dc3b8b515d5"
          }
        ],
        "requestBody": {
          "description": "Information about a new pet in the system",
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/Pet"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Return a 200 status to indicate that the data was received successfully"
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "Pet": {
        "type": "object",
        "required": [
          "id",
          "name"
        ],
        "properties": {
          "id": {
            "type": "integer",
            "format": "int64"
          },
          "name": {
            "type": "string"
          },
          "tag": {
            "type": "string"
          }
        }
      }
    },
    "securitySchemes": {
      "Signature": {
        "type": "apiKey",
        "in": "header",
        "name": "Signature",
        "description": "HMAC-SHA256 signature of the raw request body, signed with the merchant secret key and hex-encoded. The same `Signature` header is also sent by Smartpayz on outgoing webhook requests so the merchant can verify them. See [Signature Calculation](https://docs.smartpayz.com/docs/request-signature) for the full algorithm."
      },
      "X-Auth-Code": {
        "type": "apiKey",
        "in": "header",
        "name": "X-Auth-Code",
        "description": "Authentication code that identifies the merchant and authorises the request. Required on every endpoint."
      }
    }
  }
}